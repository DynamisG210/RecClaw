# Keep / Archive Matrix

Keep:

- typed memory schema and ingestion
- deterministic memory retrieval
- candidate-card validation
- deterministic policy ranking
- foundry fixture replay
- claim-boundary verifier
- BPR rank-cut diagnostic memory card

Archive:

- BPR batch-local rank-cut as a performance candidate
- retrospective judge-only proof route
- raw Stage 2/Stage 3 prompt and prediction artifacts
- full `RecClaw_Architecture` evidence zoo from runtime surfaces

The archive remains useful as evidence, but runtime behavior should come from
controlled schemas, scripts, configs, and minimal fixtures.
