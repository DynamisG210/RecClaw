Semantic-ID Generative implementation appendix. Apply these requirements only when compiled_mechanism.space_identity.search_space_id is exactly SEMANTIC_ID_GENERATIVE_MECHANISM_SPACE_V1.

The frozen LIGER construction parent is recclaw_ext.candidate.FreshCandidateModel, which subclasses TIGER and ultimately transformers.T5ForConditionalGeneration. It is not a RecBole SequentialRecommender subclass even though it exposes ModelType.SEQUENTIAL. Preserve that exact parent and its ordinary beam, legal SID resolution, dense reranking, training, and candidate-local evaluation paths except where the compiled mechanism explicitly owns a change.

- In PARENT_METHOD_PATCH_V1, return only methods listed by service_policy.parent_method_source_ownership plus candidate-only recclaw_* helpers. The materializer splices those methods into the existing FreshCandidateModel or FreshCandidateTrainer class; it does not create a subclass. Therefore an overwritten calculate_loss, predict, or full_sort_predict cannot call super() to recover the parent implementation: super() resolves to TIGER/T5, which does not own those RecClaw methods. Do not redefine an existing stable parent helper such as _dense_logits, _liger_batch, _left_padded_history, _generated_semantic_ids_and_logits, or _hybrid_scores unless that exact name is in the model-method allowlist. Do not return _build_optimizer or _train_epoch unless the exact name is in the trainer-method allowlist; an allowed _build_optimizer has the RecBole ABI _build_optimizer(self, **kwargs), not a positional model argument.
- In SCAFFOLDED_FULL_SOURCE_V1, the exact parent class, constructor, framework metadata, unchanged generator/decode, and evaluator remain machine-owned. Define the model methods in machine_owned_outer_abi.parent_hook_contract and the trainer methods in machine_owned_outer_abi.trainer_methods, plus recclaw_* helpers. Explicit OPTIMIZATION_STAGING may own complete calculate_loss, fit and _train_epoch through these same parent-method materializers; it does not require reconstructing T5 or its decode interface. CUSTOM_MODEL still owns the complete research model when generator or other nonlocal scientific components change. A POST_TRAINING capability without explicit staging remains full-source with its unresolved stage choices; it is not an additive objective by default.
- In scaffolded mode, a changed USER_CONTEXT_ENCODER uses recclaw_prepare_liger_batch(self, input_batch), where input_batch contains history inputs only and never the current positive target; the machine attaches the exact parent target SID, target item ID, and target embedding only after the hook and separately preserves the exact seen-item history. A changed DECODING_STRATEGY uses recclaw_generation_kwargs(self, input_batch) and/or recclaw_postprocess_generated(self, generated, input_batch) around the exact active self.generate call; these hooks are target-blind, never replace recclaw_generation_batch, and support only T5-native autoregressive beam or trie-constraint control. Parallel decoding and catalog-score factorization require CUSTOM_MODEL. An additive GENERATIVE_OBJECTIVE uses recclaw_additional_objective(self, interaction, outputs, logits, targets); the machine retains outputs.loss and the parent train-seen dense cross-entropy.
- For explicit OPTIMIZATION_STAGING, own stage-dependent calculate_loss, initialization/state, reference behavior and necessary fit/epoch methods. Create model stage state in recclaw_initialize_mechanism, which runs after the exact parent constructor and before optimizer creation; parameter freezing and declared phase state changes are allowed. Keep the supervised phase and implement the declared later phase separately within the fixed budget and evaluation/stopping protocol. The compiler binds the declared RECCLAW_OPTIMIZATION_STAGE_SPEC; reuse _recclaw_stage_for_update and _recclaw_apply_stage where appropriate and finish when the declared phases complete. Do not force supervised CE/dense losses into a preference-only phase. Generator preference uses the declared sampled complete SIDs and train-only reward; random catalog negatives in supervised batches are a different mechanism. Missing counts/reference remain research choices to resolve, never values supplied by the materializer.
- Learned context returned as `input_embeddings` by `recclaw_prepare_liger_batch` keeps its gradient through both native SID/ID embedding forward branches. The machine freezes original content at asset loading, not the learned hook output; preserve that live output and do not replace `model_forward` or either parent loss to work around gradient flow.
- Complete-SID likelihood objectives use the machine-owned `self.recclaw_complete_sid_token_log_probs(outputs, sids)` helper inside `recclaw_additional_objective`; call it without redefining it. `targets` is the item-ID tensor [B]; obtain absolute catalog SID tokens with `self.item_sids[item_ids]`, shaped [B,4] or [B,K,4]. The helper returns differentiable per-token log probabilities of the same shape, with each code teacher-forced on its own shifted prefix and the exact live positive history encoder state and attention mask reused. Keep gradients through the encoder and decoder; the helper preserves the parent dense query and both original losses. The mechanism owns train-only negative sampling, reduction (sum/mean as declared), weighting and margin formula. `outputs.logits` contains positive-prefix conditional distributions: gathering different negative tokens from it generally conditions on the wrong prefix and is not a complete-code likelihood scorer. Additional negative decoder work is real training cost; do not replace it with that surrogate or reduce the declared negative count to fit resources.
- For decode.autoregressive_beam_then_invalid_drop, semantic_ids are the generated shared-vocabulary catalog-absolute tokens; do not position-offset all-low, shifted, padded, or otherwise invalid tuples into legality. The compiler owns SID lookup, invalid-drop, seen filtering, deduplication, generated-item dense reranking, target ranks, predict, and full_sort_predict. The ordinary beam remains unconstrained width 20 with no catalog-prefix constraint, no length normalization, and no static fill. Parallel logits, catalog-factorized scoring, changed item resolution, or changed dense candidate membership require CUSTOM_MODEL because the parent-local T5 seam cannot implement them faithfully.
- For decode.constrained_beam_trie with tokenizer.frozen_letter_collision_suffix, the shared decoder applies the frozen item_sids catalog constraint with one batched logits processor. It preserves the candidate's beam width, length normalization and other declared generation settings, and replaces the per-beam prefix_allowed_tokens_fn callback. The ordinary unconstrained parent and other tokenizer implementations are unchanged. Keep novel research constraints explicit in their mechanism definition; do not rebuild catalog tries or synchronize a host callback separately for each beam.
- In parent-patch mode, do not return __init__. Create declared mechanism parameters, modules, and buffers in recclaw_initialize_mechanism(self, config, dataset); the machine-owned constructor wrapper invokes it after the exact parent constructor and before optimizer construction. Use recclaw_fit_operator(train_data) only for declared train-derived fitting or precomputation, not for late creation of trainable modules.
- recclaw_initialize_mechanism runs after the model is on config["device"]. Keep catalog-scale tensor kernels batched on that device. If GPU code assignments require a Python uniqueness map, transfer the complete codes tensor to CPU once, build sid_map and the complete internal_sids tensor on CPU, then copy internal_sids to the model buffer once. Never convert CUDA scalars to Python values inside an item loop and never issue per-item CUDA writes.
- In STRICT_JSON_FULL_FILE_CONTENTS mode, retain full freedom over declared scientific model changes. The compiler supplies fixed ModelType.SEQUENTIAL and InputType.POINTWISE metadata without changing the class base. When machine_owned_outer_abi.generation_hook is present, implement its recclaw_generation_batch(self, interaction, targets) interface returning generated SID tokens [B,K,4], the loss-trained query [B,128], and actual conditioning history [B,L]. targets supplies batch shape/device only. The machine consumes this hook for its listed resolver/ranking/prediction methods; writing replacement predict/full_sort_predict methods does not replace that hook. If resolution or dense scoring is itself a declared research change, follow the corresponding full-source ownership instead of the parent decoder.
- The exact parent history from _left_padded_history has shape [B, 20]. item_sids has shape [self.n_items, 4], so history semantic IDs have shape [B, 20, 4] and flatten to [B, 80]. Expand the [B, 20] history mask across the four SID positions and flatten it to [B, 80] before applying it to flattened SID tensors; append the parent's 22 zero slots to obtain [B, 102] token and attention tensors. Never broadcast a [B, 20, 1, 1] mask against a [B, 80, 128] tensor. Prefer the exact parent _liger_batch and model_forward paths instead of reconstructing this adapter.
- item_content has shape [self.n_items, 768]. The parent's emb_proj maps it to [self.n_items, 128]. Context and additive-objective mechanisms may consume the parent projection but must not replace item_sids, item_content, emb_proj, or another parent-owned field. Product quantization, learned codebooks, and changed SID assignments require CUSTOM_MODEL because geometry helpers alone do not connect them to the parent loss or generation path.
- Preserve objective.train_seen_full_catalog_dense_cross_entropy exactly in its supervised phase. Assign the mechanism's context tensor to self.predicted_embedding and reuse self._dense_logits(), including the parent's train_seen_mask, temperature, and cross-entropy semantics. Do not reconstruct the normalized full-catalog logits with @, torch.matmul, or another candidate-local matrix kernel.
- Follow resolved_ir.component_runtime_bindings exactly. EXACT_LIGER_PARENT is valid only when the complete frozen component ID, parameters, and input wiring match. Explicit stages use the complete stage-method ownership described above; post-training without a declared stage remains unresolved full-source research. Other primitives without an active parent-local seam require a declared CUSTOM_MODEL rather than disconnected helpers. Structured-field autoencoding, semantic-collaborative fusion, and dual collaborative distillation remain unavailable because this profile has no bound structured/collaborative input or teacher ABI; do not imitate those missing inputs with manifest parsing or N-by-N relations.

These are interface and unchanged-parent facts. The machine preserves stable execution machinery while the research actor controls the declared mechanism, including complete training stages and genuinely changed generators.
