BL-ICF implementation appendix. Apply these requirements only when compiled_mechanism.space_identity.search_space_id is exactly BL_ICF_MECHANISM_SPACE_V1.

The implementation must:
- for BPR_AND_EVALUATION_SCORE_V1 or EVALUATION_ONLY_SCORE_V1, define recclaw_score_head(self, user_representation, item_representation, pairwise=False). With pairwise=True return row-aligned scores of shape [batch]; otherwise return all user-block/item-block scores of shape [users, items]. Implement the declared score formula without a [users, items, embedding] intermediate. The materializer binds predict/full-sort and, only for BPR_AND_EVALUATION_SCORE_V1, the existing BPR objective to this same hook while preserving auxiliary losses, parent initialization and trainer. EVALUATION_ONLY_SCORE_V1 leaves the training objective unchanged;
- use the fixed entrypoint recclaw_ext.candidate:FreshCandidateModel;
- provide recclaw_ext.trainer:FreshCandidateTrainer as the candidate-local training entrypoint selected by execution_contract.config.recclaw_trainer_entrypoint;
- follow the exact service_policy.execution_contract in the shared blind request;
- implement every component, primitive, parameter, edge, custom component, and architecture operator declared by compiled_mechanism; do not substitute, simplify, omit, or collapse the program into a generic template;
- make every declared parameter behaviorally live at its declared computation point. A parameter must not be overwritten, used only as metadata, or algebraically cancelled by a later operation. In particular, apply view or objective weights after any normalization and in the final loss aggregation; multiplying a representation by a positive scalar immediately before normalizing it does not implement that weight;
- an objective component's `weight` scales only that declared primary objective. Never map `objective.weight` to `reg_weight`, L2, embedding-norm, or any other regularization term. In full-source, from-scratch implementations, add a new regularization loss only when compiled_mechanism contains an explicit GEOMETRY_REGULARIZATION component and use only that component's declared weight. In either exact-parent patch mode, preserve the parent's existing loss and regularization unless the corresponding objective or regularization slot is explicitly declared changed;
- satisfy every compiled_mechanism.required_capabilities item and use compiled_mechanism.resolved_ir as the executable wiring contract;
- do not define, import, re-export, or repeat RECCLAW_COMPILER_CANDIDATE_ID, RECCLAW_MECHANISM_PROGRAM_DIGEST, RECCLAW_MECHANISM_SEMANTICS_DIGEST, RECCLAW_IMPLEMENTED_COMPONENT_IDS, RECCLAW_IMPLEMENTED_COMPONENT_SPECS, RECCLAW_IMPLEMENTED_PRIMITIVE_IDS, RECCLAW_IMPLEMENTED_CUSTOM_COMPONENT_IDS, or RECCLAW_IMPLEMENTED_ARCHITECTURE_OPERATOR_IDS in any returned file. The compiler-owned materializer injects their exact module-level values into recclaw_ext/candidate.py from compiled_mechanism and compiled_mechanism.implementation_binding after this response. Executable candidate.py code may reference these names as injected module globals;
- treat execution_contract.base_model_config and execution_contract.model as authoritative, with no hidden family default or undeclared Config key;
- support the selected family as a BPR-based, LightGCN-based, or from-scratch RecBole GeneralRecommender implementation while preserving the frozen BL-ICF protocol;
- add a substantive trainable structural, interaction, representation, objective, or propagation mechanism that directly implements the supplied blind research spec;
- make the overall candidate behavior differ from the selected base model only through mechanisms declared by compiled_mechanism; predict and full_sort_predict must implement the declared score-head primitive exactly and must not add any undeclared scoring term, normalization, calibration, ensemble, or bias;
- when repair_context is present, treat repair_context.current_source_files as the authoritative edit base and return the complete revised response in the request's response_mode. Edit only the explicitly implicated method/file closure needed for the named mechanical failure. Preserve every unimplicated class, method, function, and constructor state assignment or registration exactly; even when __init__ is implicated, retain all existing state not explicitly named by the failure and keep every implicated method present. Never regenerate or rewrite the candidate from scratch. Preserve predict and full_sort_predict exactly unless that method is explicitly named by repair_context;
- keep InputType.PAIRWISE and the RecBole GeneralRecommender API;
- for `relation.item_item_cooccurrence_topk` with `efficiency.graph_filter_precomputation`, call the compiler-provided `self.recclaw_build_item_item_cooccurrence_topk(dataset)` from the constructor instead of enumerating item pairs or constructing a dense item-by-item matrix. The compiler owns sparse train-incidence enumeration and row-top-k truncation. To preserve a declared research weighting, optionally define `recclaw_item_item_cooccurrence_weight(self, counts, left_frequency, right_frequency)` as a vectorized NumPy transform; do not replace the compiler-owned sparse substrate;
- implement every non-uniform NEGATIVE_SAMPLER primitive through the compiler hook ABI and call `recclaw_sampler_step` from calculate_loss; merely consuming RecBole's supplied NEG_ITEM_ID does not implement a non-uniform sampler. For `sampler.false_negative_aware` with DYNAMIC or CURRICULUM hardness and BATCH or EPOCH refresh, define `recclaw_sampler_representations(self, epoch_idx)` returning current user/item tensors. The compiler owns legal item enumeration, padding/current-positive/train-positive masking, bounded catalog scoring, and final item-id gather; it additionally owns frozen all-user cache construction/lookup for EPOCH and current-batch-only selection for BATCH. The declared score head is used by default; define vectorized `recclaw_sampler_score_block(self, user_block, item_block, epoch_idx)` when the declared scorer, uncertainty filter, or curriculum must act over every legal score block. If the exact policy also needs selection among multiple retained legal items, define `recclaw_sampler_select_from_pool(self, user_ids, candidate_ids, candidate_scores, epoch_idx, negative_count, replacement)` and set a positive literal `self.recclaw_sampler_pool_size` large enough for that policy; return an integer tensor of shape `(user_ids.shape[0], negative_count)` drawn row-wise from `candidate_ids`. Do not implement catalog enumeration, legality masking, post-hoc id repair, shared cache construction, or lookup in candidate code;
- for sampler.uniform, prefer RecBole's supplied NEG_ITEM_ID. If any declared sampler uses recclaw_sampler_step, calculate_loss must call it; item-id outputs must preserve the interaction batch dimension, use integer dtype, and stay in [1, n_items). Mask padding id 0 before every ranking, top-k, or fallback selection, and exclude the current positive and every known positive for that user;
- implement NEGATIVE_SAMPLER refresh_frequency literally. For DYNAMIC or CURRICULUM hardness, selected negatives must causally depend on current trainable model state; CURRICULUM selection must also depend on epoch progression. A LOW-cost sampler must not scan the full user-by-item space or use CPU `tolist` per-example loops unless that work is explicitly declared and costed. The compiler-owned model-dependent false-negative path performs frozen refresh/lookup for EPOCH and fresh current-batch selection for BATCH through the hooks above. For every other EPOCH sampler, define recclaw_sampler_refresh(self, epoch_idx), compute and freeze the complete epoch negative cache there, and make recclaw_sampler_step a side-effect-free lookup into that cache until the next refresh: two calls with the same interaction must return the same tensor, and recclaw_sampler_step must not advance or mutate a cursor, counter, queue, generator, or cache. Key the frozen cache by stable interaction content such as (user id, positive item id), not by call order. FreshCandidateTrainer must call model.recclaw_sampler_refresh(epoch_idx) exactly once at the start of every _train_epoch before its first batch. Do not recompute hardness or negative selection inside recclaw_sampler_step for an EPOCH sampler. For every other BATCH sampler, refresh inside recclaw_sampler_step for each batch and do not reuse an epoch cache;
- implement the selected TRAINING_PROCEDURE in FreshCandidateTrainer. FreshCandidateTrainer must subclass recbole.trainer.Trainer and directly define the RecBole-compatible method `def _build_optimizer(self, **kwargs)`: obtain `params` from `kwargs.pop("params", self.model.parameters())`, obtain the declared optimizer settings from the remaining kwargs or the corresponding trainer attributes, and return the exact declared optimizer at the exact declared learning rate. RecBole calls this method with no positional `params` argument during Trainer initialization. For any procedure other than plain training.adam, training.sgd, or training.adamw, directly define recclaw_training_step and call it from a directly overridden _train_epoch or fit on every executed epoch/transition; that hook must perform the declared schedule, stage, curriculum, freezing, clipping, precision, checkpointing, restart, or batch/negative-budget behavior. Do not rely on RecBole defaults for a declared training primitive;
- if FreshCandidateTrainer directly overrides `_train_epoch`, it must accept the frozen RecBole call ABI `_train_epoch(train_data, epoch_idx, loss_func=None, show_progress=False)`, including the `loss_func` and `show_progress` keyword arguments;
- use only Python, PyTorch, NumPy, SciPy, and RecBole already present in the frozen runtime;
- use public SciPy sparse APIs; frozen SciPy 1.15.3 does not provide dok_matrix._update;
- for STRICT_JSON_FULL_FILE_CONTENTS provide the complete three-file package; for PARENT_METHOD_PATCH_V1 provide only the model/trainer method fragments allowed by the common contract; for PARENT_LOCAL_SLOT_PATCH_V1 provide only the compiler-declared typed model-hook fragment and leave the parent trainer untouched;
- keep all tensors device-safe; before every sparse operation, move each relation or adjacency operand to the active embedding operand's device at the call site instead of relying on initialization-time placement. A SciPy sparse matrix has no `.to(...)` or `.to_sparse()` method: convert it once to a coalesced torch sparse tensor with the selected LightGCN helper when that helper is inherited, or explicitly from its COO row, column, and data arrays, then move the torch tensor to the active device. An item-item relation has shape `(self.n_items, self.n_items)` and may multiply only item embeddings; propagation over concatenated user and item embeddings requires a `(self.n_users + self.n_items, self.n_users + self.n_items)` adjacency. Do not mask NaN or infinity with nan_to_num, replacement constants, or equivalent fallback behavior. If an operation becomes nonfinite, let qualification or training reject it, then repair the mathematics without changing the declared mechanism;
- work for both batched pairwise training interactions and full-sort user-only interactions.

RecBole Config is not a dict and has no get method. Do not call config.get(...). A permitted key may be present with value None, so normalize None to the declared literal default before numeric use. Read only keys permitted by execution_contract; candidate-only mechanism constants must be literal code defaults, not undeclared Config tuning. In full-source mode FreshCandidateModel.__init__(self, config, dataset) must call the selected base class initializer. In either parent patch mode do not return __init__; use recclaw_initialize_mechanism(self, config, dataset) for mechanism-only initialization, except for the objective-owned initializer described below. After initialization, model cardinalities and device are `self.n_users`, `self.n_items`, and `self.device`; the Dataset exposes `dataset.user_num` and `dataset.item_num`, not `dataset.n_users` or `dataset.n_items`. In PARENT_METHOD_PATCH_V1 define calculate_loss, predict, and full_sort_predict directly on FreshCandidateModel only when those methods own a declared delta. In PARENT_LOCAL_SLOT_PATCH_V1 do not redefine them: the machine-owned parent routes all relevant paths through the compiler-declared typed hook. Do not assume attributes supplied by another model family.

For PARENT_LOCAL_SLOT_PATCH_V1 with PRIMARY_LOSS_WITH_PARENT_VIEWS_V1, return an unbased FreshCandidateModel fragment defining recclaw_primary_objective(self, interaction, representations), optional recclaw_initialize_primary_objective(self, config, dataset), and necessary recclaw_* helpers. Put imports inside methods. representations is the exact parent forward tuple: all-user embeddings, all-item embeddings, and, when present, the parent's shallow and propagated views. Return the complete new scalar primary loss, including its declared state updates; this replaces the old primary computation, not an additive term or a multiplier-only template. The parent graph, forward, auxiliary losses, prediction and trainer execute unchanged. Initialize objective-owned recclaw_* state and train-only precomputations in recclaw_initialize_primary_objective, which runs once after the parent constructor and is replaced on the next objective change. Do not define recclaw_initialize_mechanism for this slot or invoke the old calculate_loss. Use the supplied representations without repeating unchanged propagation. A parent without this separable loss interface uses complete source implementation instead.

Forbidden: configuration-only changes, parameter tuning as the mechanism, wrappers around unchanged base behavior, fixed-catalog lookup, static candidate selection, fallback behavior, mock code, smoke-only stubs, edits to RecBole or RecClaw shared code, filesystem/network access, side/origin branching, and claimed outcomes.
