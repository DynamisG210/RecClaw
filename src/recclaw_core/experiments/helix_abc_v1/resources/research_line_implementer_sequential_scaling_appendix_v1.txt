Sequential-Scaling implementation appendix. Apply these requirements only when compiled_mechanism.space_identity.search_space_id is exactly SEQUENTIAL_SCALING_MECHANISM_SPACE_V1.

The frozen construction parent is the exact SSD4Rec RecBole SequentialRecommender bundle. Preserve its constructor, mask-token training, padded-prefix geometry, tied full-catalog scoring, trainer, and every unchanged method. A parent-relative candidate implements only the compiler-declared mechanism hook and must not rebuild unrelated parent behavior.

- In PROFILE_MODEL_HOOKS_V1, return exactly the hooks named by service_policy.profile_model_source_ownership.model_hooks, optional recclaw_initialize_mechanism(self, config, dataset), and mechanism-local recclaw_* helpers only. Never return __init__, forward, calculate_loss, predict, full_sort_predict, trainer methods, timestamp extraction, final-state gather, or a query-token append. The machine invokes the initializer once after the exact parent constructor, prepares the exact parent embeddings and valid mask, aligns elapsed time, gathers item_seq_len-1, and preserves every undeclared score/objective path.
- Source ownership follows the compiled component graph. recclaw_sequence_representation runs only at the declared representation edge before BiSSD. The machine then always executes the complete exact BiSSD parent. recclaw_sequence_state runs only after that parent backbone and receives (parent_state, item_embeddings, item_seq, item_seq_len, elapsed_days, query_elapsed_days, valid_mask, user_ids=None); optional item, time, query-time, representation, and user inputs are None when the changed graph does not declare them. It returns only a residual delta, which the machine adds to parent_state before masking padding; do not return parent_state itself or reconstruct the backbone. recclaw_sequence_features is not a PROFILE_MODEL_HOOKS_V1 hook. Any scientifically necessary backbone, compression, or execution replacement must use CUSTOM_MODEL with explicit synthesis and the honest full-source architecture path.
- The parent interaction ABI is self.ITEM_SEQ for padded item prefixes, self.ITEM_SEQ_LEN for their valid lengths, self.POS_ITEM_ID for the next training target, and self.ITEM_ID for requested-item prediction. calculate_loss returns one scalar, forward returns one [B, hidden_size] prefix state, full_sort_predict returns [B, self.n_items], and predict returns one score per requested row.
- Chronological order is owned by chrono_order and chrono_order_list. TRAIN_PREFIX_TIMESTAMP_SEQUENCE values come from timestamp_list. Construct the final query gap from timestamp only when the compiled component also declares QUERY_TIMESTAMP; a prefix-only component must not invent that signal. Raw timestamp values never own split or ordering identity. Do not silently substitute chrono_order values for declared raw-time intervals.
- Treat padded suffix positions as nonexistent. Build adjacent gaps only between valid consecutive prefix events, build a final query gap only for a component that declares QUERY_TIMESTAMP, and set every padding step to an inert zero before applying log, exponent, recurrence, attention, or normalization. Never compute an invalid padded branch and then hide it with torch.where.
- For state.time_aware_forgetting, read the required compiler-owned component parameter time_scale_seconds=86400.0. First subtract the UNIX-second timestamps to obtain a finite nonnegative valid raw gap, then divide that gap exactly once by time_scale_seconds so the decay rate operates per elapsed day before any exponent or sigmoid. Do not learn, infer, replace, omit, or apply this scale before subtraction.
- For RAW_DIFFERENCE, transform the nonnegative valid raw gap directly. For LOG1P_DIFFERENCE, establish a finite nonnegative valid raw gap before torch.log1p. Apply the compiled dt_min and dt_max to valid transformed steps in that order; do not ignore, replace, or reinterpret either bound. A negative gap at a valid chronological position is an informative protocol error, not a value to mask.
- Only an explicit CUSTOM_MODEL whole-backbone proposal may implement backbone.continuous_time_s5 or another replacement architecture. In that full-source path, create dedicated trainable state-space parameters with the declared layer, feature, and state widths; realize spectrum [state_width], input projection [feature_width,state_width], output projection [state_width,feature_width], and recurrent activation [B,state_width] per token; and implement the declared stability and ZOH mathematics literally. Never materialize [B,feature_width,state_width] or present a replacement backbone as a parent-preserving profile hook.
- Do not use nan_to_num, isfinite-to-zero, broad try/except, parent fallback, or post-hoc clipping to conceal nonfinite states, losses, scores, or gradients. Repair the interval construction or mechanism mathematics and preserve the declared candidate.
- A repair revision changes only the implicated mechanism methods. It must preserve every unrelated exact-parent method and every already-correct declared component.

These are exact parent ABI, chronology, padding, and numerical semantics. Ordinary profile candidates preserve the active BiSSD parent and retain research freedom in the supported temporal, state-gating, and interest-routing mechanisms. Whole-backbone research remains available only through an explicit CUSTOM_MODEL full-source proposal.
