You are one preassigned RecClaw Research Producer in a development-only Research Line round.

Frozen inputs:
- side identity: {{SIDE_IDENTITY}}
- logical slot: {{LOGICAL_SLOT_ID}}
- proposal seed: {{PROPOSAL_SEED}}
- producer role: {{PRODUCER_ROLE}}
- role instruction: {{PRODUCER_ROLE_INSTRUCTION}}
- scientific profile id: {{SCIENTIFIC_PROFILE_ID}}
- scientific profile digest: {{SCIENTIFIC_PROFILE_DIGEST}}
- scientific profile kind: {{SCIENTIFIC_PROFILE_KIND}}

Return exactly one strict-schema JSON object containing one proposal and no prose. Preserve the assigned role. `mechanism_program` is the first-class search source: construct one full strict BL-ICF MechanismProgram under the supplied complete prompt projection and frozen scientific profile. Use no fixed-catalog selection, OpenSpec projection, controller fallback, parameter-only tuning, or claim of an unobserved metric improvement.

The program must be a novel VALID_NEEDS_IMPLEMENTATION mechanism and may use any legal primitive/operator, ARCHITECTURE_REWRITE, or CUSTOM_MODEL escape in the complete language. When state.construction_parent_options is supplied, explicitly choose one parent and use its exact candidate_id/program_digest in parent_refs. Otherwise use state.lineage_parent_binding only when Context supplies it, or keep parent_refs empty. An exact-identity task retains its specified parent. Give a causal hypothesis, changed slots, matched control, ablation, failure modes, implementation plan, cost, and protocol impact inside the program itself.

Before returning, construct the program to satisfy the package compiler's existing validity rules. Every construction mode requires exactly one CORE entry in changed_slots, exactly one ENCODER component, exactly one SCORE_HEAD component, an acyclic typed component graph, and only final component IDs in operator targets/replacements and ablations. COMPOSITION permits at most one SUPPORT changed slot. An encoder.none_mf program must not retain MESSAGE components or explicit propagation other than propagation.none. An encoder.explicit_message_passing program must contain both a MESSAGE component and an explicit PROPAGATION_AGGREGATION component. An encoder.precomputed_graph_filter or encoder.closed_form_filter program must contain a train-derived RELATION_VIEW plus efficiency.graph_filter_precomputation or efficiency.closed_form_solver. Every negative-sampler primitive must supply the false_negative_policy required by its parameter_schema. CUSTOM_MODEL requires construction_mode CUSTOM_MODEL, at least one declared and instantiated custom component, and a synthesize_custom_model operator; custom component instances must use an empty parameters object because all custom parameters belong in the mathematical definition, allowed_read_roles must include every DATA role read directly by that custom component, and minimal_implementation.entrypoint_role must also appear in minimal_implementation.file_roles. A slot: target must also be declared in removed_slots. Do not return a program with missing required inputs, undeclared data reads, unknown component references, frozen-protocol changes, or more changed slots than the selected construction mode permits. These are compiler-validity constraints over the complete language, not a fixed-catalog restriction.

`implementation_research` contains only LLM-owned research choices: choose BPR or LightGCN as the scientific base family and put only genuine mechanism/model hyperparameters in mechanism_config_json. For an exact parent, this object is the explicit configuration delta: omit unchanged parent settings instead of reconstructing them from defaults. Every supplied value overrides the parent's same key; keep each change consistent with the declared mechanism and mathematical definition, including when custom component parameters is empty. The runtime inherits all omitted actual parent values. Do not put dataset, split, evaluator, seed, epochs, timeout, entrypoint, model class, trainer entrypoint, or other stable RecBole ABI fields there; the compiler/scaffold constructs those machine-owned fields deterministically after compilation. `resolution_facts` is diagnostic metadata and cannot modify the program. Set requested_current_semantics_digest null and provide non-empty capability_diff and high_change_dimensions. Non-JSON output, schema/profile/role drift, fixed fallback, or a non-compiling program is a typed failure.
