Diffusion-Flow CF implementation appendix. Apply these requirements only when compiled_mechanism.space_identity.search_space_id is exactly DIFFUSION_FLOW_CF_MECHANISM_SPACE_V1.

The frozen parent uses RecBole DiffRec's GeneralRecommender and AutoEncoderMixin lifecycle with InputType.LISTWISE. Preserve that family contract while implementing the compiled mechanism at its declared computation point.

- In PROFILE_MODEL_HOOKS_V1, return exactly the hooks named by service_policy.profile_model_source_ownership.model_hooks, optional recclaw_initialize_mechanism(self, config, dataset), and mechanism-local recclaw_* helpers only. The possible DiffRec hook signatures are recclaw_predict_field(self, state, timestep, condition=None), recclaw_diffusion_state(self, history), recclaw_diffusion_decode(self, state), and recclaw_diffusion_condition(self, inputs, parameters, *, training). Never return __init__, forward, calculate_loss, p_mean_variance, p_sample, predict, full_sort_predict, a forward path, prior, target or inverse, a recovery solver, a trajectory witness, or score calibration.
- recclaw_diffusion_state returns a finite batch-aligned tensor on the history device. For state.spectral_graph_coordinates it returns [B, rank] coordinates, and recclaw_diffusion_decode maps recovered coordinates back to [B, self.n_items] using the same machine-selected basis. spectral_index_ranges are ascending, disjoint, zero-based half-open column ranges in the descending positive spectrum of the binary train Gram R^T R; their lengths sum to rank, and adjacent intervals must already be merged in the draft. The machine computes top-K with K=max range end, then selects those columns. Deeper selections increase precompute cost at fixed rank. [0,rank) explicitly selects the leading subspace. A full-width projection history@basis@basis.T is not a low-rank state. Consume self.recclaw_spectral_basis directly for both encode and decode; its columns are already selected, so do not select twice or construct a replacement basis. Old frequency_band labels have no mapping in new drafts and require scientific revision, not automatic translation. The machine sets self.recclaw_state_dimension from rank and constructs the parent's time-conditioned DNN with matching input/output width, preserving hidden widths, time embedding, activation, normalization and dropout. For state.learned_latent_autoencoder, recclaw_diffusion_state returns E(history) with shape [B, latent_dimension]; recclaw_diffusion_decode returns D(state) with shape [B, self.n_items]. The machine sets self.recclaw_state_dimension from latent_dimension and constructs matching parent DNN endpoints. Implement the declared encoder/decoder layers and all their trainable parameters in recclaw_initialize_mechanism. The machine adds reconstruction_weight * mean((D(E(history)) - history)^2) once to the scalar diffusion loss, outside timestep importance and SNR weighting; keep gradients live through both hooks. Do not decode before VP corruption or add a separate reconstruction-loss hook. A zero reconstruction_weight disables this auxiliary term. Other currently bound state profiles retain the history shape. The machine applies the state hook during training and inference and retains Gaussian-VP corruption, the clean-state x0 objective, SNR reweighting, loss-history updates and posterior recovery in state coordinates; inference decoding occurs once after recovery, before full-catalog scoring.
- The parent-x0 profile has no independently mutable forward-path or prior hook. A non-parent corruption path or a prior-to-data path changes the training distribution and therefore cannot reuse DiffRec's clean-state target and Gaussian posterior solver by assumption. Such a mechanism requires an explicit CUSTOM_MODEL that jointly owns a mathematically matched forward path, target, inverse, recovery solver, and their executable trajectory.
- Source ownership follows the compiled primitive graph, not the COMPOSITION or ARCHITECTURE_REWRITE label. The supported behavior-guided VELOCITY plus flow-matching graph keeps the exact VP path x_t = alpha*x_0 + sigma*epsilon while the compiler owns target v = alpha*epsilon - sigma*x_0, inverse x_0 = alpha*x_t - sigma*v, the unchanged DiffRec posterior solver, recovered-logit score, and real intermediate-state trajectory. The hook owns only the learned field.
- A supported parent-x0 field graph keeps the exact parent state, VP forward path, linear schedule, clean-state target, importance sampler, SNR reweighting, loss-history update, deterministic posterior solver, score, and trajectory. When CONDITIONING is declared, recclaw_diffusion_condition receives the compiled DATA/component inputs and declared parameters; its actual output reaches the field during training and recovery. Apply training-only masking or unconditional dropout only when training=True; inference uses its declared inference condition. The mechanism-off witness passes None to the field. Use compiler-bound self.recclaw_spectral_basis at its declared rank when present; do not construct or replace it. Only the field, its local condition computation, and mechanism parameters belong in recclaw methods.
- The profile binder calls the exact parent constructor through the protected materialized class. Put mechanism-only initialization in recclaw_initialize_mechanism; the outer machine lifecycle invokes it once after parent construction and cardinality binding.
- A custom field that preserves all six other DiffRec parent slots uses PROFILE_MODEL_HOOKS_V1 even when the program is labeled CUSTOM_MODEL. Implement recclaw_diffusion_field(self, inputs, parameters, *, training) when that hook is requested. inputs contains the exact declared field ports, including this batch's history, noisy state, timestep and machine-fitted train-only graph as declared. Return the same shape as inputs["state"]; use its actual width including padding. The machine calls this field in both the parent loss and every parent recovery step. Define the new field mathematics and its local parameters only. Normalize a train graph according to the declared math; compute repeated sparse propagation without materializing its dense square.
- Initialize all trainable field parameters in recclaw_initialize_mechanism before optimizer construction. Train-graph fitting happens after this initializer; consume inputs["item_graph"] in the field and cache its normalized sparse operator on first use. The graph is raw symmetric train-item co-occurrence, without self edges and with an empty padding row and column. Do not cache a batch's history or propagated history across unrelated users.
- Full-source mode is reserved for a true CUSTOM_MODEL with an explicit synthesize_custom_model operator. In that mode, call the selected base-class initializer before using framework state. The constructor may read dataset.user_num and dataset.item_num. After base initialization, model cardinalities and placement are self.n_users, self.n_items, and self.device. There is no self.user_num or self.item_num.
- interaction[self.USER_ID] is a one-dimensional user tensor of length B. self.get_rating_matrix(user) returns the dense observed-interaction state x_start with shape [B, self.n_items] on the model device. calculate_loss returns one scalar, full_sort_predict returns full-catalog scores with shape [B, self.n_items], and predict gathers the requested item score for each of the B rows.
- If a mechanism operates on observed positives, let positive_pairs = x_start.nonzero(as_tuple=False), whose shape is [P, 2]. Use positive_pairs[:, 0] as batch-row indices and positive_pairs[:, 1] as item indices, and gather pair values with x_start[row_index, item_index]. A per-positive local neighborhood or sample table has shape [P, K]. Never index the full state with repeated positive-row indices (for example x_start[positive_rows]), because that creates [P, self.n_items] rather than preserving [B, self.n_items].
- Keep working memory proportional to O(B * self.n_items + P * K), in addition to declared model state, where K is a bounded local neighborhood, sample count, or low-rank width. Do not materialize O(P * self.n_items), O(B * self.n_items * self.n_items), or a dense item-by-item matrix. Use sparse, low-rank, blockwise, or indexed operations for graph, spectral, positive-conditioned, and guidance mechanisms while keeping gradients live.
- State remains batch-aligned throughout corruption, field prediction and recovery: [B, rank] for the compiler-bound spectral-coordinate profile, [B, latent_dimension] for state.learned_latent_autoencoder, and [B, self.n_items] for the full-state profiles. A CUSTOM_MODEL may explicitly own another matched latent path and training objective. Every low-dimensional path must decode back to [B, self.n_items] before full-catalog scoring. Timestep tensors remain [B], and scalar schedule coefficients broadcast over the state rather than duplicating catalog rows.
- Repair revisions must preserve every unimplicated parent method and candidate mechanism. Correct interface or tensor geometry without replacing the declared scientific idea with the frozen parent or a simpler fallback.
