# Generic baseline defaults for custom architecture proposals.
# Loaded when base_model is Custom or an unrecognized architecture family.
embedding_size: 64
learning_rate: 0.001
reg_weight: 1e-05
num_layers: 2
ffn_hidden_size: 128
max_seq_len: 50
positional_encoding: sinusoidal
